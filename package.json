{
  "name": "token-page-landing",
  "version": "1.0.0",
  "scripts": {
    "start": "everypage serve --clean --directory ./site --output-directory ./dist",
    "build": "everypage build --clean --directory ./site --output-directory ./dist"
  },
  "main": "index.js",
  "dependencies": {
    "@kibalabs/everypage": "^0.9.6",
    "@kibalabs/everypage-cli": "^0.9.6"
  }
}
